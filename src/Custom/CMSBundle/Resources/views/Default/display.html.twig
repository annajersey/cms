{% extends '::base.html.twig' %}

{% block body %}

        <div class="page">
            <h1>{{ page.title }}</h1>
            <h4>Category: {{ page.category.name }}</h4>
            <p>{{ page.content }}<p>
        </div>

{% endblock %}